import {
  Platform,
  StyleSheet,
  type StyleProp,
  type TextStyle,
} from 'react-native';
import { normalize } from '@/utils/normalize';
import type { ThemeColors } from './types';

export function createMessageItemStyles(colors: ThemeColors, width: number) {
  return StyleSheet.create({
    slackThreadContainer: {
      flexDirection: 'row',
      alignItems: 'center',
      marginTop: 0,
      marginBottom: 18,
      paddingVertical: 4,
      paddingHorizontal: 10,
      backgroundColor: colors.threadBarBackground,
      borderRadius: 16,
      minHeight: 36,
      elevation: 1,
      shadowColor: colors.black,
      shadowOpacity: 0.04,
      shadowRadius: 1,
    },
    replyBarSent: {
      alignSelf: 'flex-end',
      marginRight: 8,
    },
    replyBarReceived: {
      alignSelf: 'flex-start',
      marginLeft: 8,
    },
    participantStack: {
      flexDirection: 'row',
      alignItems: 'center',
      marginRight: 8,
    },
    threadAvatar: {
      width: 24,
      height: 24,
      borderRadius: 12,
      borderWidth: 2,
      borderColor: colors.white,
      backgroundColor: colors.surfaceElevated,
    },
    threadText: {
      color: colors.textMuted,
      fontWeight: '600',
      marginRight: 4,
    },
    threadCountExtra: {
      color: colors.textSecondary,
      marginLeft: 2,
      fontWeight: '500',
    },
    messageWrapper: { maxWidth: '85%' },
    sentWrapper: { alignSelf: 'flex-end' },
    receivedWrapper: { alignSelf: 'flex-start' },
    bubble: {
      padding: 4,
      borderRadius: 12,
      elevation: 0.5,
      shadowColor: colors.black,
      shadowOpacity: 0.04,
      shadowRadius: 1,
    },
    sentBubble: {
      backgroundColor: colors.chatSentBubble,
      borderTopRightRadius: 2,
    },
    receivedBubble: {
      backgroundColor: colors.chatReceivedBubble,
      borderTopLeftRadius: 2,
    },
    verticalMediaStack: { flexDirection: 'column', gap: 4, marginBottom: 2 },
    mediaContainer: {
      position: 'relative',
      backgroundColor: colors.border,
      borderWidth: 0.5,
      borderColor: colors.border,
      borderRadius: 10,
    },
    videoWrapper: {
      width: width * 0.75,
      height: 220,
      borderRadius: 10,
      overflow: 'hidden',
      backgroundColor: colors.black,
    },
    mediaImage: { width: width * 0.75, height: 220, borderRadius: 10 },
    videoPlayOverlay: {
      ...StyleSheet.absoluteFillObject,
      justifyContent: 'center',
      alignItems: 'center',
      backgroundColor: 'rgba(0,0,0,0.1)',
    },
    playIconCircle: {
      width: 50,
      height: 50,
      borderRadius: 25,
      backgroundColor: 'rgba(0,0,0,0.5)',
      justifyContent: 'center',
      alignItems: 'center',
      paddingLeft: 4,
    },
    mediaTimeOverlay: {
      position: 'absolute',
      bottom: 8,
      right: 8,
      backgroundColor: 'rgba(0,0,0,0.4)',
      paddingHorizontal: 6,
      paddingVertical: 2,
      borderRadius: 10,
      flexDirection: 'row',
      alignItems: 'center',
    },
    mediaTimeText: { color: colors.white },
    receiptIconSmall: {
      width: 14,
      height: 14,
      marginLeft: 3,
      tintColor: colors.primary,
    },
    voiceContainer: {
      flexDirection: 'row',
      alignItems: 'center',
      width: width * 0.72,
      paddingVertical: 10,
      paddingHorizontal: 6,
    },
    playBtn: {
      width: 30,
      height: 30,
      justifyContent: 'center',
      alignItems: 'center',
    },
    playIcon: { color: colors.textSecondary },
    waveformContainer: {
      flex: 1,
      height: 35,
      justifyContent: 'center',
      marginHorizontal: 8,
    },
    waveformLine: {
      height: 2,
      backgroundColor: colors.waveformInactive,
      width: '100%',
      borderRadius: 1,
    },
    playbackThumb: {
      width: 10,
      height: 10,
      borderRadius: 5,
      position: 'absolute',
      left: '0%',
    },
    voiceDuration: {
      position: 'absolute',
      bottom: -2,
      left: 0,
      color: colors.messageMeta,
    },
    voiceAvatarContainer: {
      position: 'relative',
      width: 36,
      height: 36,
      marginLeft: 4,
    },
    voiceAvatar: { width: 36, height: 36, borderRadius: 18 },
    micBadge: {
      position: 'absolute',
      bottom: -2,
      right: -2,
      width: 14,
      height: 14,
      borderRadius: 7,
      justifyContent: 'center',
      alignItems: 'center',
    },
    miniMic: { width: 8, height: 8, tintColor: colors.white },
    complexFileWrapper: {
      backgroundColor: 'rgba(0,0,0,0.03)',
      borderRadius: 8,
      padding: 10,
      width: width * 0.75,
      marginBottom: 4,
    },
    fileMainRow: { flexDirection: 'row', alignItems: 'center' },
    fileIconBox: {
      width: 42,
      height: 42,
      borderRadius: 6,
      justifyContent: 'center',
      alignItems: 'center',
      elevation: 1,
    },
    fileExtText: { color: colors.white, fontWeight: 'bold', fontSize: 10 },
    fileInfo: { flex: 1, marginLeft: 12 },
    fileNameText: {
      fontSize: 14,
      color: colors.textPrimary,
      fontWeight: '500',
    },
    fileMetaText: { color: colors.messageMeta, marginTop: 2 },
    messageText: {
      color: colors.textPrimary,
      fontSize: normalize(15),
      lineHeight: normalize(21),
      paddingHorizontal: 8,
      paddingVertical: 4,
    },
    mentionText: {
      color: colors.primary,
      fontSize: normalize(15),
      lineHeight: normalize(21),
    },
    linkText: {
      color: colors.primary,
      fontSize: normalize(15),
      lineHeight: normalize(21),
      textDecorationLine: 'underline',
    },
    messageFooter: {
      flexDirection: 'row',
      alignSelf: 'flex-end',
      alignItems: 'center',
      marginTop: 2,
      paddingRight: 8,
      paddingBottom: 4,
    },
    timeText: { color: colors.messageMeta },
    receiptIcon: {
      width: 16,
      height: 16,
      marginLeft: 4,
      tintColor: colors.primary,
    },
    dateHeader: {
      alignSelf: 'center',
      backgroundColor: colors.dateHeaderBackground,
      paddingHorizontal: 12,
      paddingVertical: 4,
      borderRadius: 8,
      marginVertical: 15,
    },
    dateText: {
      color: colors.messageMeta,
      fontSize: 12,
      fontWeight: '500',
    },
    reactionBadge: {
      position: 'absolute',
      bottom: -12,
      backgroundColor: colors.reactionBackground,
      paddingHorizontal: 6,
      paddingVertical: 2,
      borderRadius: 12,
      borderWidth: 2,
      borderColor: colors.background,
      flexDirection: 'row',
      alignItems: 'center',
      elevation: 2,
      shadowColor: colors.black,
      shadowOpacity: 0.1,
      shadowRadius: 2,
    },
    sentReactionPos: { right: 10 },
    receivedReactionPos: { left: 10 },
    reactionEmojiRow: { flexDirection: 'row', alignItems: 'center' },
    reactionEmoji: {
      fontSize: 13,
      backgroundColor: colors.reactionBackground,
      borderRadius: 10,
    },
    reactionCountText: {
      marginLeft: 2,
      color: colors.messageMeta,
      fontWeight: '600',
    },
    replyContainer: {
      backgroundColor: 'rgba(0,0,0,0.05)',
      padding: 8,
      borderRadius: 8,
      borderLeftWidth: 4,
      borderLeftColor: colors.primary,
      marginBottom: 5,
      marginHorizontal: 4,
      marginTop: 4,
    },
    threadAction: {
      flexDirection: 'row',
      alignItems: 'center',
      alignSelf: 'flex-start',
      marginTop: 10,
    },
  });
}

/** Group DM message layout — extends DM styles with row/avatar layout. */
export function createGroupMessageItemStyles(
  colors: ThemeColors,
  width: number,
) {
  const base = createMessageItemStyles(colors, width);
  return StyleSheet.create({
    ...base,
    rowContainer: { flexDirection: 'row' },
    receivedRow: {
      alignSelf: 'flex-start',
      alignItems: 'flex-start',
      paddingLeft: 10,
      marginTop: 15,
    },
    sentRow: {
      alignSelf: 'flex-end',
      alignItems: 'flex-end',
      paddingRight: 10,
      marginTop: 15,
    },
    groupAvatarContainer: { marginRight: 8 },
    groupSenderAvatar: {
      width: 30,
      height: 30,
      borderRadius: 15,
      borderWidth: 1,
      borderColor: colors.border,
      objectFit: 'contain',
    },
    senderNameLabel: { paddingHorizontal: 8, paddingTop: 4, paddingBottom: 2 },
  });
}

/** Channel message layout — Slack-style row with inline reactions. */
export function createChannelMessageItemStyles(
  colors: ThemeColors,
  width: number,
) {
  const base = createMessageItemStyles(colors, width);
  return StyleSheet.create({
    ...base,
    slackThreadContainer: {
      flexDirection: 'row',
      alignItems: 'center',
      marginTop: 2,
      marginBottom: 4,
      paddingVertical: 2,
      paddingHorizontal: 10,
      borderRadius: 16,
      minHeight: 32,
      alignSelf: 'flex-start',
    },
    rowContainer: {
      flexDirection: 'row',
      alignItems: 'flex-start',
      paddingLeft: 10,
      marginBottom: 20,
    },
    messageColumn: { flex: 1, minWidth: 0, paddingRight: 10 },
    groupAvatarContainer: { marginRight: 5 },
    groupSenderAvatar: {
      width: 35,
      height: 35,
      borderRadius: 50,
      borderWidth: 1,
      borderColor: colors.border,
    },
    senderNameLabel: { minWidth: 0, marginRight: 4 },
    senderRow: {
      flexDirection: 'row',
      alignItems: 'center',
      width: '100%',
      paddingHorizontal: 8,
      paddingTop: 4,
      paddingBottom: 2,
    },
    messageWrapper: { maxWidth: '100%', marginTop: -3 },
    mediaContainer: {
      position: 'relative',
      borderWidth: 0.5,
      borderColor: colors.border,
      borderRadius: 10,
    },
    verticalMediaStack: {
      flexDirection: 'column',
      gap: 4,
      marginBottom: 2,
      marginTop: 5,
      maxWidth: '100%',
      alignSelf: 'flex-start',
    },
    mediaImage: {
      width: width * 0.75,
      height: 220,
      borderRadius: 10,
    },
    videoWrapper: {
      width: width * 0.75,
      height: 220,
      borderRadius: 10,
      overflow: 'hidden',
      backgroundColor: colors.black,
    },
    complexFileWrapper: {
      backgroundColor: 'rgba(0,0,0,0.03)',
      borderRadius: 8,
      padding: 10,
      width: width * 0.65,
      marginBottom: 4,
    },
    messageText: {
      color: colors.textPrimary,
      fontSize: 15,
      lineHeight: 21,
      paddingHorizontal: 8,
      paddingVertical: 4,
    },
    mentionText: { color: colors.primary, fontSize: 15, lineHeight: 21 },
    channelMentionText: {
      color: colors.channelMentionForeground,
      backgroundColor: colors.channelMentionBackground,
      fontSize: 15,
      lineHeight: 21,
    },
    linkText: {
      color: colors.primary,
      fontSize: 15,
      lineHeight: 21,
      textDecorationLine: 'underline',
    },
    reactionContainer: {
      flexDirection: 'row',
      flexWrap: 'wrap',
      paddingHorizontal: 8,
      marginTop: 4,
    },
    reactionBadge: {
      flexDirection: 'row',
      alignItems: 'center',
      backgroundColor: colors.reactionBackground,
      borderRadius: 12,
      paddingHorizontal: 6,
      paddingVertical: 2,
      marginRight: 4,
      marginBottom: 4,
      borderWidth: 1,
      borderColor: colors.transparent,
    },
    reactionBadgeActive: {
      backgroundColor: colors.surfaceElevated,
      borderColor: colors.primary,
    },
    reactionCount: { marginLeft: 4, color: colors.messageMeta },
    reactionTextActive: { color: colors.primary, fontWeight: 'bold' },
  });
}

export function createTypingUsersStyles(colors: ThemeColors) {
  return StyleSheet.create({
    container: {
      position: 'absolute',
      left: 16,
      bottom: '100%',
      paddingBottom: 4,
      zIndex: 1,
      pointerEvents: 'none',
    },
    text: {
      fontSize: 12,
      color: colors.messageMeta,
      fontStyle: 'italic',
    },
  });
}

export function createChatInputStyles(colors: ThemeColors) {
  return StyleSheet.create({
    inputAreaWrapper: {
      position: 'relative',
      backgroundColor: colors.topNavigation,
    },
    inputContainer: {
      flexDirection: 'row',
      alignItems: 'center',
      padding: 8,
      paddingVertical: 20,
      backgroundColor: colors.topNavigation,
    },
    textInputWrapper: {
      flex: 1,
      flexDirection: 'row',
      backgroundColor: colors.surface,
      borderRadius: 25,
      marginHorizontal: 8,
      paddingHorizontal: 12,
      paddingVertical: 4,
      alignItems: 'center',
    },
    input: {
      flex: 1,
      paddingTop: Platform.OS === 'ios' ? 10 : 8,
      paddingBottom: Platform.OS === 'ios' ? 10 : 8,
      fontSize: 16,
      color: colors.textPrimary,
    },
    composerMeasureText: {
      position: 'absolute',
      opacity: 0,
      left: 0,
      top: 4,
      fontSize: 16,
      lineHeight: 20,
      paddingTop: Platform.OS === 'ios' ? 10 : 8,
      paddingBottom: Platform.OS === 'ios' ? 10 : 8,
      color: colors.textPrimary,
    },
    inputOverlay: {
      position: 'absolute',
      left: 0,
      right: 36,
      top: 0,
      bottom: 0,
      paddingVertical: 8,
      zIndex: 1,
    },
    inputTransparentText: {
      color: 'transparent',
    },
    composerMentionText: {
      color: colors.primary,
      fontSize: 16,
      lineHeight: 22,
    },
    composerChannelMentionText: {
      color: colors.channelMentionForeground,
      backgroundColor: colors.channelMentionBackground,
      fontSize: 16,
      lineHeight: 22,
    },
    emojiBtn: { padding: 4 },
    recordingBar: {
      flex: 1,
      flexDirection: 'row',
      alignItems: 'center',
      justifyContent: 'space-between',
      backgroundColor: colors.surface,
      borderRadius: 25,
      marginHorizontal: 8,
      paddingHorizontal: 16,
      height: 44,
      overflow: 'hidden',
    },
    timerRow: { flexDirection: 'row', alignItems: 'center' },
    redDot: {
      width: 8,
      height: 8,
      borderRadius: 4,
      backgroundColor: colors.error,
      marginRight: 8,
    },
    timerText: { fontSize: 16, color: colors.textPrimary },
    slideHint: { color: colors.textSecondary, fontSize: 14 },
    voiceBar: {
      flex: 1,
      flexDirection: 'row',
      alignItems: 'center',
      backgroundColor: colors.surface,
      borderRadius: 25,
      marginHorizontal: 8,
      paddingVertical: 8,
      paddingHorizontal: 10,
      height: 52,
    },
    previewPlayBtn: {
      width: 34,
      height: 34,
      borderRadius: 17,
      backgroundColor: colors.primary,
      justifyContent: 'center',
      alignItems: 'center',
      marginRight: 8,
    },
    waveformRow: {
      flex: 1,
      flexDirection: 'row',
      alignItems: 'center',
      gap: 2,
      height: 28,
    },
    wavebar: { flex: 1 },
    previewTimer: { color: colors.messageMeta, marginLeft: 8, minWidth: 30 },
    iconBtn: { padding: 8 },
    inputIcon: { width: 24, height: 24, tintColor: colors.iconDefault },
    rightIcons: { flexDirection: 'row', alignItems: 'center' },
    sendBtn: {
      width: 40,
      height: 40,
      borderRadius: 22,
      backgroundColor: colors.primary,
      justifyContent: 'center',
      alignItems: 'center',
    },
    sendBtnDisabled: { opacity: 0.6 },
    sendIcon: {
      width: 18,
      height: 18,
      objectFit: 'contain',
      tintColor: colors.white,
    },
  });
}

/** Styles for TipTap HTML rendered via react-native-markdown-display. */
export function createMessageMarkdownStyles(
  colors: ThemeColors,
  textStyle?: StyleProp<TextStyle>,
) {
  const flatText = StyleSheet.flatten(textStyle) || {};
  const fontSize = flatText.fontSize ?? normalize(15);
  const lineHeight = flatText.lineHeight ?? normalize(21);
  const color = (flatText.color as string) || colors.textPrimary;

  const bodyPadding = {
    ...(flatText.paddingHorizontal != null && {
      paddingHorizontal: flatText.paddingHorizontal,
    }),
    ...(flatText.paddingVertical != null && {
      paddingVertical: flatText.paddingVertical,
    }),
    ...(flatText.paddingLeft != null && { paddingLeft: flatText.paddingLeft }),
    ...(flatText.paddingRight != null && {
      paddingRight: flatText.paddingRight,
    }),
    ...(flatText.paddingTop != null && { paddingTop: flatText.paddingTop }),
    ...(flatText.paddingBottom != null && {
      paddingBottom: flatText.paddingBottom,
    }),
    ...(flatText.marginTop != null && { marginTop: flatText.marginTop }),
    ...(flatText.marginBottom != null && {
      marginBottom: flatText.marginBottom,
    }),
  };

  return {
    body: {
      color,
      fontSize,
      lineHeight,
      ...bodyPadding,
    },
    text: {
      color,
      fontSize,
      lineHeight,
    },
    paragraph: {
      color,
      fontSize,
      lineHeight,
      marginTop: 0,
      marginBottom: 0,
      flexWrap: 'wrap' as const,
    },
    strong: {
      fontWeight: '700' as const,
      color,
    },
    em: {
      fontStyle: 'italic' as const,
      color,
    },
    s: {
      textDecorationLine: 'line-through' as const,
      color,
    },
    del: {
      textDecorationLine: 'line-through' as const,
      color,
    },
    link: {
      color: colors.primary,
      fontSize,
      lineHeight,
      textDecorationLine: 'underline' as const,
    },
    mention: {
      color: colors.primary,
      fontSize,
      lineHeight,
      fontWeight: '600' as const,
    },
    channelMention: {
      color: colors.channelMentionForeground,
      backgroundColor: colors.channelMentionBackground,
      fontSize,
      lineHeight,
      fontWeight: '600' as const,
    },
    bullet_list: {
      marginTop: 4,
      marginBottom: 4,
    },
    ordered_list: {
      marginTop: 4,
      marginBottom: 4,
    },
    list_item: {
      flexDirection: 'row' as const,
      justifyContent: 'flex-start' as const,
      marginVertical: 2,
    },
    bullet_list_icon: {
      color,
      fontSize,
      lineHeight,
      marginLeft: 0,
      marginRight: 8,
    },
    ordered_list_icon: {
      color,
      fontSize,
      lineHeight,
      marginLeft: 0,
      marginRight: 8,
    },
    code_inline: {
      backgroundColor: colors.surfaceElevated,
      color,
      fontFamily: Platform.OS === 'ios' ? 'Menlo' : 'monospace',
      fontSize: fontSize - 1,
      paddingHorizontal: 4,
      paddingVertical: 1,
      borderRadius: 4,
    },
    fence: {
      backgroundColor: colors.surfaceElevated,
      borderRadius: 8,
      padding: 10,
      marginVertical: 6,
      overflow: 'hidden' as const,
    },
    code_block: {
      backgroundColor: colors.surfaceElevated,
      color,
      fontFamily: Platform.OS === 'ios' ? 'Menlo' : 'monospace',
      fontSize: fontSize - 1,
    },
    blockquote: {
      backgroundColor: colors.surfaceElevated,
      borderLeftColor: colors.primary,
      borderLeftWidth: 3,
      paddingHorizontal: 10,
      paddingVertical: 4,
      marginVertical: 4,
    },
    heading1: { color, fontSize: fontSize + 6, fontWeight: '700' as const },
    heading2: { color, fontSize: fontSize + 4, fontWeight: '700' as const },
    heading3: { color, fontSize: fontSize + 2, fontWeight: '700' as const },
  };
}
